<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Infinite Linear Loading Animation</title>
    <style>
        .loader {
            width: 100%;
            height: 10px; /* Adjust the height of the line */
            overflow: hidden;
            background-color: #f0f0f0;
            position: relative;
        }

        .loader::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, #3498db, transparent);
            animation: loading 2s infinite linear;
        }

        @keyframes loading {
            from {
                left: -100%;
            }
            to {
                left: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="loader"></div>
</body>
</html>
